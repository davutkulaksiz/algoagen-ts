// (A) -> (B) -> (C) -> (D)     node<T>
//  ^                           val: T
//  | (head)                    next?: node<T>
// get(i)

// Singly Linked List (A points B, B points C, C points D)
// you can't walk backwards

// double linked list comes with additional proprety: prev?: node<T>